(() => {
  const NS = 'dreamcord-sillytavern-bridge';
  console.log(`[${NS}] UI extension loaded.`);
  console.log(
    `[${NS}] This package includes a server plugin in /sillytavern-plugin. ` +
    `Install that folder into SillyTavern /plugins for character sync endpoints.`
  );
})();
